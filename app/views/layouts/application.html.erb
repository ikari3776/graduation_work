<!DOCTYPE html>
<html>
  <head>
    <title><%= page_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="d-flex align-items-center">
        <div class="dropdown">
          <button class="btn btn-light btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            メニュー
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/">ホーム</a></li>   
            <li><a class="dropdown-item" href="#">プレイ</a></li>
            <li><a class="dropdown-item" href="#">ランキング</a></li>
            <li><a class="dropdown-item" href="#">遊び方</a></li>   
          </ul>
        </div>
      </div>

      <% if logged_in? %>
        <%= button_to "ログアウト", logout_path, method: :delete, class: "btn btn-danger btn-sm" %>      
      <% else %>
        <%= link_to "ユーザー登録", new_user_path, class: "btn btn-success btn-sm" %>      
        <%= link_to "ログイン", login_path, class: "btn btn-success btn-sm" %>      
      <% end %>

      <div class="mx-auto text-center">
        <p class="navbar-brand text-white m-0">検索王</p>
      </div>

      <div class="position-absolute bottom-0 end-0 p-2">
        <% if logged_in? %>
          <p class="text-white small mb-0">ログイン中: <%= current_user.name %>さん</p>
        <% else %>
          <p class="text-white small mb-0">ログインしていません</p>
        <% end %>
      </div>
    </nav>

    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>

    <%= yield %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </body>
</html>
