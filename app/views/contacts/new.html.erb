<% content_for(:title, 'お問い合わせ') %>
<h2 class="text-center">お問い合わせ</h2>
<div class="text-center">
  <p>以下のフォームに必要事項をご記入の上、送信してください。</p>
  <p>内容を確認後、emailにてご連絡いたします。</p>
</div>
<div class="container-fluid light-blue-container px-5">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <%= form_with model: @contact, url: confirm_contacts_path, local: true do |f| %>
        <div class="form-group">
          <%= f.label :name, '名前（ペンネーム可）' %><span class="text-danger">*</span>
          <%= f.text_field :name, autofocus: true, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :email, 'メールアドレス（返信をご希望の方はご記入ください）' %>
          <%= f.email_field :email, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :subject, '件名' %><span class="text-danger">*</span>
          <%= f.text_field :subject, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :message, '内容' %><span class="text-danger">*</span>
          <%= f.text_area :message, size: '10x10', class: 'form-control' %>
        </div>

        <div>
          <%= f.submit '入力内容確認', class: "btn btn-primary" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
