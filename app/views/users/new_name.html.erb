<h2>ユーザー名を入力してください</h2>

<%= form_with model: @user, url: create_name_path, local: true do |f| %>
    <% if @user.errors.any? %>
    <div class="error-messages">
    <h2><%= @user.errors.count %>件のエラーがあります:</h2>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name, "ユーザー名" %><span class="text-danger">*</span>
    <%= f.text_field :name, placeholder: '半角英数字のみ使用できます', class: "form-control", required: true, minlength: 3, maxlength: 20 %>
    <small class="form-text text-muted">
      ※ユーザー名は後から変更可能です。<br>
      ※ランキングに掲載される場合、他のユーザーにも公開されます。
    </small>  
  </div>

  <%= f.submit "保存する", class: "btn btn-primary mt-3" %>
<% end %>
